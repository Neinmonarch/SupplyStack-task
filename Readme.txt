Project run via npm, so you need to install it

List of commands to use:

Run application
```bash npm run start```

Linting
```bash npm run lint```

Formatting
```bash npm run format```

Testing
```bash npm run test```